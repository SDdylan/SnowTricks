{% extends 'base.html.twig' %}

{% form_theme resetForm 'bootstrap_5_layout.html.twig' %}

{% block title %}Changez votre mot de passe{% endblock %}

{% block body %}
<div id="password">
    <section>
    <h1 id="password-title">Changez votre mot de passe</h1>
        <div class="container form-control form-password">
            {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <div id="btn-password">
                <button class="btn btn-primary" type="submit">Réinitialiser mot de passe</button>
            </div>
            {{ form_end(resetForm) }}
        </div>
    </section>
</div>
{% endblock %}

