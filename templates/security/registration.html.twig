{% extends "base.html.twig" %}

{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}
    <div id="register">
        <section>
            <h1 id="register-title">Inscription</h1>
            <!--<h2 id="register-subtitle">Rejoignez dès à présent la communauté de Snow Tricks en remplissant ce formulaire !</h2>-->
            <div class="container form-control form-register" style="background: none; height: max-content">
                {{ form_start(form) }}
                {{ form_row(form.username, {'attr': {'placeholder': "Pseudo"}}) }}
                {{ form_row(form.email, {'attr': {'placeholder': "Email"}}) }}
                {{ form_row(form.lastName, {'attr': {'placeholder': "Nom"}}) }}
                {{ form_row(form.firstName, {'attr': {'placeholder': "Prénom"}}) }}
                {{ form_row(form.imageUrl, {'attr': {'placeholder': "Image URL"}}) }}
                {{ form_row(form.password, {'attr': {'placeholder': "Mot de passe"}}) }}
                {{ form_row(form.confirm_password, {'attr': {'placeholder': "Confirmez mot de passe"}}) }}
                {# {{ form_widget(form) }} #}
                <button id="btn-register" type="submit" class="btn btn-success btn-lg">Inscription</button>
                {{ form_end(form) }}
            </div>
        </section>
    </div>
{% endblock %}